# Python
backend/venv/
backend/.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment variables (secrets) - only *.env.example files are committed
.env
.env.*
!.env.example

# Node
frontend/node_modules/
frontend/dist/
frontend/coverage/
*.tsbuildinfo

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Build / temporary files
*.tmp
*.temp

# Files this Cowork session drops into the project
Claude outputs/

# Ingested repositories (Phase 4): user code, never committed
backend/workspaces/

# Knowledge sources (Phase 7): MITRE's and OWASP's published files, downloaded
# rather than vendored — they have their own licences and their own release
# cadence, and a 10 MB catalogue does not belong in this history.
knowledge-sources/
# The embedding model, downloaded once and cached on first use.
backend/models/
