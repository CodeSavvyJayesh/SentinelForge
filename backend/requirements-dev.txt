# Development / test tooling. Install: pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.0.3
# HTTP client used by FastAPI/Starlette TestClient (Starlette 1.6 prefers httpx2).
httpx2
ruff==0.15.11
